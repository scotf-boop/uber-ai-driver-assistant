Render部署说明

Build Command:
pip install -r requirements.txt

Start Command:
python app.py

Google Maps:
如需启用Google Maps，在Render环境变量中添加：
GOOGLE_MAPS_API_KEY=你的Google Maps API Key

注意：
V12 OCR使用浏览器端Tesseract.js，需要浏览器能访问CDN。
